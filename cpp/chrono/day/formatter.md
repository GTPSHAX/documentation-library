---
title: std::formatter<std::chrono::day>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/day/formatter
---


# formatterpetty|<std::chrono::day>

ddcl|header=chrono|1=
template< class CharT >
struct formatter<std::chrono::day, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::day`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


---
title: std::formatter<std::chrono::local_info>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/local_info/formatter
---


# formattersmall|<std::chrono::local_info>

ddcl|header=chrono|since=c++20|
template< class CharT >
struct formatter<std::chrono::local_info, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::local_info`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


## See also


| cpp/utility/format/dsc format | (see dedicated page) |


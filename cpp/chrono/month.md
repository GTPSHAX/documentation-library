---
title: std::chrono::month
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/month
---


```cpp
**Header:** `<`chrono`>`
dcl|since=c++20|
class month;
}
}
}
}
}
}
}
}
}
}
}
}
```

The class `month` represents a month in a year. Its normal range is , but it may hold any number in . Twelve named constants are predefined in the `std::chrono` namespace for the twelve months of the year.
`month` is a *TriviallyCopyable* *StandardLayoutType*.

## Member functions


| cpp/chrono/month/dsc constructor | (see dedicated page) |
| cpp/chrono/month/dsc operator inc dec | (see dedicated page) |
| cpp/chrono/month/dsc operator arith | (see dedicated page) |
| cpp/chrono/month/dsc operator unsigned | (see dedicated page) |
| cpp/chrono/month/dsc ok | (see dedicated page) |


## Nonmember functions


| cpp/chrono/month/dsc operator cmp | (see dedicated page) |
| cpp/chrono/month/dsc operator arith 2 | (see dedicated page) |
| cpp/chrono/month/dsc operator ltlt | (see dedicated page) |
| cpp/chrono/month/dsc from_stream | (see dedicated page) |


## Helper classes


| cpp/chrono/dsc formatter|month | (see dedicated page) |
| cpp/chrono/month|nested=true|notes= | |


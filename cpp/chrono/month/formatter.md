---
title: std::formatter<std::chrono::month>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/month/formatter
---


# formatterpetty|<std::chrono::month>

ddcl|header=chrono|since=c++20|
template< class CharT >
struct formatter<std::chrono::month, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::month`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


## Defect reports


## See also


| cpp/utility/format/dsc format | (see dedicated page) |


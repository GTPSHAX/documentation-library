---
title: std::formatter<std::chrono::weekday>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/weekday/formatter
---


# formattersmall|<std::chrono::weekday>

ddcl|header=chrono|since=c++20|
template< class CharT >
struct formatter<std::chrono::weekday, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::weekday`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


## See also


| cpp/utility/format/dsc format | (see dedicated page) |


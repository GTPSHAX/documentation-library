---
title: std::formatter<std::chrono::weekday_indexed>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/weekday_indexed/formatter
---


# formattersmall|<std::chrono::weekday_indexed>

ddcl|header=chrono|since=c++20|
template< class CharT >
struct formatter<std::chrono::weekday_indexed, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::weekday_indexed`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


## See also


| cpp/utility/format/dsc format | (see dedicated page) |


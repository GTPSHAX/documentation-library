---
title: std::formatter<std::chrono::year>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/year/formatter
---


# formattersmall|<std::chrono::year>

ddcl|header=chrono|1=
template< class CharT >
struct formatter<std::chrono::year, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::year`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


## See also


| cpp/utility/format/dsc format | (see dedicated page) |


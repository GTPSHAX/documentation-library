---
title: std::formatter<std::chrono::year_month_day>
type: Utilities
source: https://en.cppreference.com/w/cpp/chrono/year_month_day/formatter
---


# formattersmall|<std::chrono::year_month_day>

ddcl|header=chrono|since=c++20|
template< class CharT >
struct formatter<std::chrono::year_month_day, CharT>;
Specialization of `std::formatter` that defines formatting rules for a `std::chrono::year_month_day`.
The `std::formatter` specialization is usually not directly accessed, but is used through formatting functions.

## Format specification


## Example


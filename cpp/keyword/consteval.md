---
title: keyword: consteval
type: Keywords
source: https://en.cppreference.com/w/cpp/keyword/consteval
---


## Usage

* `consteval` declaration specifier
* ''lambda-declarator'' that explicitly specifies the function call to be an immediate function
rrev|since=c++23|
* `consteval` `if` statement

## Notes


## See also

* `if` statement: `if`, `else`
* `constexpr` `if` statement: `constexpr` <small>(`constexpr` `if` statement)</small>
* `switch` statement: `case`, `default` <small>(as the declaration of the default case label)</small>, `switch`
* `goto` statement: `goto`
* `continue` statement: `continue`
* `break` statement: `break`
* `return` statement: `return`, `co_return`
* `do-while` loop and `while` loop: `do`, `while`
* `for` loop and range-based `for` loop: `for`

---
title: keyword: constexpr
type: Keywords
source: https://en.cppreference.com/w/cpp/keyword/constexpr
---


## Usage

* `constexpr` declaration specifier
rrev|since=c++17|
* `constexpr` `if` statement
* ''lambda-declarator'' that explicitly specifies the function call to be a `constexpr` function

## See also

* `if` statement: `if`, `else`
rrev|since=c++23|
* `consteval` `if` statement: `consteval`
* `switch` statement: `case`, `default` <small>(as the declaration of the default case label)</small>, `switch`
* `goto` statement: `goto`
* `continue` statement: `continue`
* `break` statement: `break`
* `return` statement: `return`<sup>(since C++20)</sup> , `co_return`
* `do-while` loop and `while` loop: `do`, `while`
* `for` loop and range-based `for` loop: `for`

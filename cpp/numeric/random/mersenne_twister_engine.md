---
title: std::mersenne_twister_engine
type: Numerics
source: https://en.cppreference.com/w/cpp/numeric/random/mersenne_twister_engine
---


```cpp
**Header:** `<`random`>`
dcl|since=c++11|1=
template<
class UIntType, std::size_t w, std::size_t n, std::size_t m, std::size_t r,
UIntType a, std::size_t u, UIntType d, std::size_t s,
UIntType b, std::size_t t, UIntType c, std::size_t l, UIntType f
> class mersenne_twister_engine;
```

`mersenne_twister_engine` is a random number engine based on [Mersenne Twister](https://en.wikipedia.org/wiki/Mersenne Twister) algorithm. It produces high quality, but not cryptographically secure, unsigned integer random numbers of type `UIntType` on the interval }.

## Template parameters


### Parameters

- `w` - the power of two that determines the range of values generated by the engine
- `n` - the degree of recurrence
- `m` - the middle word, an offset used in the recurrence relation defining the state
- `r` - the number of bits of the lower bit-mask, also known as the twist value
- `a` - the conditional xor-mask, i.e. the coefficients of the rational normal form twist matrix
- `u, d, s, b, t, c, l` - the 1 to 7 components of the bit-scrambling (tempering) matrix
- `f` - the initialization multiplier
If any of the following restrictions is violated, the program is ill-formed:
* `m` is in .
* The following expressions are all `true`:
:* `1=w >= 3`
:* `1=w >= r`
:* `1=w >= u`
:* `1=w >= s`
:* `1=w >= t`
:* `1=w >= l`
:* `1=w <= std::numeric_limits<UIntType>::digits`
* Given `(1u << w) - 1u` as `w1`, the following expressions are all `true`:
:* `1=a <= w1`
:* `1=b <= w1`
:* `1=c <= w1`
:* `1=d <= w1`
:* `1=f <= w1`

## Generator properties

The `size` of the states of `mersenne_twister_engine` is `n`, each of them consists of a sequence `X` of `n` values of type `result_type`.  stands for the th value (starting from 0) of `X`.
Given the following bitwise operation notations:
* }, built-in bitwise AND.
* }, built-in bitwise XOR.
* }, built-in bitwise left-shift.
* }, built-in bitwise right-shift.
The `transition algorithm` of `mersenne_twister_engine` ()) is defined as follows:
# Concatenate the upper `w - r` bits of } with the lower `r` bits of } to obtain an unsigned integer value `Y`.
# Let `y` be }, and set  to mathjax-or|\(\scriptsize X_{i+m−n}\ \mathsf{xor}\ (Y\ \mathsf{rshift}\ 1)\ \mathsf{xor}\ y\)|X xor (Y rshift 1) xor y.
The `generation algorithm` of `mersenne_twister_engine` ()) is defined as follows:
# Let  be mathjax-or|\(\scriptsize X_i\ \mathsf{xor}\ ((X_i\ \mathsf{rshift}\ u)\ \mathsf{bitand}\ d)\)|X xor ((X rshift u) bitand d).
# Let  be mathjax-or|\(\scriptsize z_1\ \mathsf{xor}\ (((z_1\ \mathsf{lshift}\ s)\mod 2^w)\ \mathsf{bitand}\ b)\)|X xor (((X lshift s) mod 2) bitand b).
# Let  be mathjax-or|\(\scriptsize z_2\ \mathsf{xor}\ (((z_2\ \mathsf{lshift}\ t)\mod 2^w)\ \mathsf{bitand}\ c)\)|X xor (((X lshift t) mod 2) bitand c).
# Let  be mathjax-or|\(\scriptsize z_3\ \mathsf{xor}\ (z_3\ \mathsf{rshift}\ l)\)|z xor (z rshift l).
# Deliver  as the result (i.e. )=z).

## Predefined specializations

The following specializations define the random number engine with two commonly used parameter sets:


| Item | Description |
|------|-------------|
| random | |
| **Type** | Definition |
| cpp/numeric/random/dsc mt19937 | (see dedicated page) |
| cpp/numeric/random/dsc mt19937_64 | (see dedicated page) |


## Nested types


| Item | Description |
|------|-------------|
| **Type** | Definition |


## Data members


## Member functions


#### Construction and Seeding

| cpp/numeric/random/engine/dsc constructor|mersenne_twister_engine | (see dedicated page) |
| cpp/numeric/random/engine/dsc seed|mersenne_twister_engine | (see dedicated page) |

#### Generation

| cpp/numeric/random/engine/dsc operator()|mersenne_twister_engine | (see dedicated page) |
| cpp/numeric/random/engine/dsc discard|mersenne_twister_engine | (see dedicated page) |

#### Characteristics

| cpp/numeric/random/engine/dsc min|mersenne_twister_engine | (see dedicated page) |
| cpp/numeric/random/engine/dsc max|mersenne_twister_engine | (see dedicated page) |


## Non-member functions


| cpp/numeric/random/engine/dsc operator_cmp|mersenne_twister_engine | (see dedicated page) |
| cpp/numeric/random/engine/dsc operator_ltltgtgt|mersenne_twister_engine | (see dedicated page) |


## Example


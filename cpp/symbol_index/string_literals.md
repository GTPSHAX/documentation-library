---
title: std::literals::string_literals Symbol Index
type: Symbol index
source: https://en.cppreference.com/w/cpp/symbol_index/string_literals
---


# std::literals::string_literals Symbol Index mark since c++14

This page tries to list all the symbols that are available from the  in the namespace `std::literals::string_literals`. The symbols are written as follows:
* Function names with `()`.
* Templates with `<>`.

## Notes

`std::chrono::duration` also defines `operator""s` to represent literal seconds, but it is an arithmetic literal: `10.0s` and `10s` are ten seconds, but `"10"s` is a string.

# <blockquote>`S`</blockquote>

<br>

## S

`cpp/string/basic_string/operator""s|s` <sup>(C++14)</sup><br>
